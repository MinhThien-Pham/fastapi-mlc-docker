services:
  web:
    build: .
    ports:
      - '8000:8000'
    volumes:
      - .:/app
      - mlc_workspace:/workspace
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

volumes:
  mlc_workspace:
